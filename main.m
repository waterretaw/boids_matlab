clear; clc;
b = Boids();
b.run();

% need to do --have done
% 坐标轴固定 axis([xmin xmax ymin ymax]); axis manual;
% move方向 angel(x+yi)
% 向量旋转 makehgtform

% need to do 
% 动画效果的优化 movie
% 转化为矩阵的形式描述
